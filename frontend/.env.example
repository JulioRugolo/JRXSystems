# =============================================================================
# Frontend JRX Sistemas (Vite)
# =============================================================================
# Desenvolvimento: deixe vazio — o Vite encaminha /api e /health para o backend
# (veja vite.config.js → proxy para 127.0.0.1:3001).
#
# Produção / Railway: URL pública da API, sem barra no final (igual jjtech / torres).
# VITE_API_URL=https://sua-api.railway.app

VITE_API_URL=

# Opcional — URL do site (meta, links absolutos futuros)
# VITE_APP_URL=http://localhost:5173
